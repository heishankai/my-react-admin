import type { ModuleType } from '@/types';

/**
 * @description 弹窗类型枚举
 */
export const MODULE_TYPE_MAP: Record<ModuleType, string> = {
  add: '新增',
  edit: '修改',
  detail: '详情',
  increase: '增加',
};
